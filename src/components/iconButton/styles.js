import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  iconsFeedback: {
    height: 35,
    width: 35,
    margin: 5,
  },

  containerIcons: {
    flexDirection: 'row',
  },
});

export default styles;
